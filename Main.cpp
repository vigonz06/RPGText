#include "GraphInter.h"
#include "PlaceList.h"
#include "Player.h"

void main()
{
	/*PlaceList* places = new PlaceList;
	Player* player = new Player;
	Places* place = new Places;
	int first, second;

	places->load("Places.txt");

	GraphInter::load();
	GraphInter::get()->clearConsole();
	first = GraphInter::get()->mainMenu();
	GraphInter::get()->clearConsole();

	switch (first)
	{
	case 1:

		if (!player->load())
		{
			GraphInter::get()->display("Player could not be load");
			GraphInter::get()->pause();
			
			place = places->buscar(0);
		}
		else
		{
			place = places->buscar(player->mapa);
		}
		break;
	}

	do
	{
		switch (second)
		{
			//lo ideal seria que todos los menus tuvieran el mismo numero de opciones
		}
		//bucle del juego
		//va a ser chungo
		//precisa de centinela de terminacion
		//hay que modificar el menu para poder salir y guardar
	} while (second != 5 //5 es un placeholder);*/
}